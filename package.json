{
  "name": "deep-check",
  "version": "1.0.0",
  "description": "check for the existence of deeply nested objects",
  "main": "index.js",
  "scripts": {
    "test": "node test.js"
  },
  "dependencies": {
    "deep-check": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.6.3"
  }
}
