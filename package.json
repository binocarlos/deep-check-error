{
  "name": "deep-check-error",
  "version": "1.0.0",
  "description": "Run deep-check and throw and Error for missing properties",
  "main": "index.js",
  "scripts": {
    "test": "node test.js"
  },
  "dependencies": {
    "deep-check": "^1.1.0"
  },
  "devDependencies": {
    "tape": "^4.6.3"
  }
}
